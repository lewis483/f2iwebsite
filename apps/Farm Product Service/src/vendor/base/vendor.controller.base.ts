/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import { VendorService } from "../vendor.service";
import { VendorCreateInput } from "./VendorCreateInput";
import { Vendor } from "./Vendor";
import { VendorFindManyArgs } from "./VendorFindManyArgs";
import { VendorWhereUniqueInput } from "./VendorWhereUniqueInput";
import { VendorUpdateInput } from "./VendorUpdateInput";
import { FarmProductFindManyArgs } from "../../farmProduct/base/FarmProductFindManyArgs";
import { FarmProduct } from "../../farmProduct/base/FarmProduct";
import { FarmProductWhereUniqueInput } from "../../farmProduct/base/FarmProductWhereUniqueInput";

export class VendorControllerBase {
  constructor(protected readonly service: VendorService) {}
  @common.Post()
  @swagger.ApiCreatedResponse({ type: Vendor })
  async createVendor(@common.Body() data: VendorCreateInput): Promise<Vendor> {
    return await this.service.createVendor({
      data: data,
      select: {
        contact: true,
        createdAt: true,
        id: true,
        productsSupplied: true,
        updatedAt: true,
        vendorName: true,
      },
    });
  }

  @common.Get()
  @swagger.ApiOkResponse({ type: [Vendor] })
  @ApiNestedQuery(VendorFindManyArgs)
  async vendors(@common.Req() request: Request): Promise<Vendor[]> {
    const args = plainToClass(VendorFindManyArgs, request.query);
    return this.service.vendors({
      ...args,
      select: {
        contact: true,
        createdAt: true,
        id: true,
        productsSupplied: true,
        updatedAt: true,
        vendorName: true,
      },
    });
  }

  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: Vendor })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async vendor(
    @common.Param() params: VendorWhereUniqueInput
  ): Promise<Vendor | null> {
    const result = await this.service.vendor({
      where: params,
      select: {
        contact: true,
        createdAt: true,
        id: true,
        productsSupplied: true,
        updatedAt: true,
        vendorName: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: Vendor })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async updateVendor(
    @common.Param() params: VendorWhereUniqueInput,
    @common.Body() data: VendorUpdateInput
  ): Promise<Vendor | null> {
    try {
      return await this.service.updateVendor({
        where: params,
        data: data,
        select: {
          contact: true,
          createdAt: true,
          id: true,
          productsSupplied: true,
          updatedAt: true,
          vendorName: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: Vendor })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async deleteVendor(
    @common.Param() params: VendorWhereUniqueInput
  ): Promise<Vendor | null> {
    try {
      return await this.service.deleteVendor({
        where: params,
        select: {
          contact: true,
          createdAt: true,
          id: true,
          productsSupplied: true,
          updatedAt: true,
          vendorName: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Get("/:id/farmProducts")
  @ApiNestedQuery(FarmProductFindManyArgs)
  async findFarmProducts(
    @common.Req() request: Request,
    @common.Param() params: VendorWhereUniqueInput
  ): Promise<FarmProduct[]> {
    const query = plainToClass(FarmProductFindManyArgs, request.query);
    const results = await this.service.findFarmProducts(params.id, {
      ...query,
      select: {
        createdAt: true,
        id: true,
        price: true,
        productName: true,
        quantity: true,
        updatedAt: true,

        vendor: {
          select: {
            id: true,
          },
        },
      },
    });
    if (results === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return results;
  }

  @common.Post("/:id/farmProducts")
  async connectFarmProducts(
    @common.Param() params: VendorWhereUniqueInput,
    @common.Body() body: FarmProductWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      farmProducts: {
        connect: body,
      },
    };
    await this.service.updateVendor({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Patch("/:id/farmProducts")
  async updateFarmProducts(
    @common.Param() params: VendorWhereUniqueInput,
    @common.Body() body: FarmProductWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      farmProducts: {
        set: body,
      },
    };
    await this.service.updateVendor({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Delete("/:id/farmProducts")
  async disconnectFarmProducts(
    @common.Param() params: VendorWhereUniqueInput,
    @common.Body() body: FarmProductWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      farmProducts: {
        disconnect: body,
      },
    };
    await this.service.updateVendor({
      where: params,
      data,
      select: { id: true },
    });
  }
}
